class Main {
    static class Node{
        int val;
        Node left;
        Node right;
        Node(int val){
            this.val = val;
        }
    }
    public static void main(String[] args) {
        Node root = new Node(1);  //root initialized with 1 and then left and right nodes will be creted and filled with any number.
        root.left = new Node(2);
        root.right = new Node(3);
        root.left.left = new Node(4);
        root.left.right = new Node(5);
        root.right.left = new Node(6);
        root.right.right = new Node(7);
        //     1
        //    / \
        //   2   3
        //  / \ / \
        // 4  5 6  7
        // In this way the tree will be linked
    }
}
Time Complexity - O(1) to add nodes 
