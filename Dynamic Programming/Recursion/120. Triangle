class Solution {
    public int minimumSum(List<List<Integer>> triangle,int i,int j){
        if(i==triangle.size()-1){
            return triangle.get(i).get(j);
        }
        int down = triangle.get(i).get(j) + minimumSum(triangle,i+1,j);
        int right = triangle.get(i).get(j) + minimumSum(triangle,i+1,j+1);
        return Math.min(down,right);
    }
    public int minimumTotal(List<List<Integer>> triangle) {
        int n = triangle.size();
        return minimumSum(triangle,0,0);
    }
}
TIME COMPLEXITY - O(2**m*n)
SPACE COMPLEXITY - O(m*n)
