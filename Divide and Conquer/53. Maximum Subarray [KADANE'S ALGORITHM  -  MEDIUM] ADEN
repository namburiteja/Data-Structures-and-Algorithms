class Solution {
    public int maxSubArray(int[] nums) {
        int maxi = Integer.MIN_VALUE;
        int sum = 0;
        for(int i=0;i<nums.length;i++){
            sum+=nums[i];
            if(sum>maxi) maxi = sum;
            if(sum<0){
                sum = 0;
            }
        }
        return maxi;
    }
}
//Here we maintain sum variable which will maintain the sum of all integers from 0 to n but if the sum becomes less than zero even after adding then we don't consider its maxi value and replace sum as with 0. 
